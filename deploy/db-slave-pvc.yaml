apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-slave-pvc
  labels:
    app: postgres
    role: slave
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
